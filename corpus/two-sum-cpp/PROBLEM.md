# two-sum (corpus problem definition)

Input:  line 1: n and target (space-separated). Line 2: n integers.
Output: two 1-based indices i < j with a[i] + a[j] == target.
Guarantee: exactly one valid pair exists. n <= 2*10^5.
All solutions below solve exactly this spec.
